import React from 'react'

class UserSnippet extends React.Component{

    render(){
        return (
            <div className="user-snip">
                {/* <img src="#" alt={this.props.user.username} />
                <h4><Link to={`/users/${this.props.user.id}`} >{this.props.user.username}</Link></h4> */}
            </div>
        )
    }
}

export default UserSnippet;